package com.management.farm.domain.port;

public interface CropDefinitionServiceGateway {

    //TODO: If time allowed, would have defined contracts that were going to be implemented by the CropDefinitionServiceGatewayImpl so as to enforce the Hexagonal Architecture so that it would be easier to swap out implementations for outside dependencies or providers. The same goes for all classes in the port package and infrastructure.adapter.gateway package
}
